#Write a Python program to convert ASCII value to character.
ascii_value = int(input("Enter ASCII value: "))

character = chr(ascii_value)

print("Character for ASCII value", ascii_value, "is", character)